import { css } from 'styled-components'

export const CommonTextMixin = css`
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 0.4px;
  color: var(--gray-dark);
`

export const CommonLargeTitleMixin = css`
  font-size: 24px;
  letter-spacing: 0.2px;
  font-weight: bold;
  color: var(--gray-darker);
`

export const CommonTitleMixin = css`
  font-size: 18px;
  letter-spacing: 0.8px;
  font-weight: bold;
  color: var(--gray-darker);
`
