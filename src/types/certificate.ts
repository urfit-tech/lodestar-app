export type Certificate = {
  id: string
  title: string
  abstract: string
  code: string
  expiredAt: string
  certificateId: string
  member: {
    name: string
  }
  category: string
  hours: number
  createdAt: string
  distributedAt: string
  template: string
}
