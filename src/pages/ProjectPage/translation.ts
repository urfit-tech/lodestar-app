import { defineMessages } from 'react-intl'

const ProjectPageMessages = {
  PreOrderPage: defineMessages({
    defaultPreOrderContent: {
      id: 'page.PreOrderPage.defaultPreOrderContent',
      defaultMessage: 'Default Pre Order Content',
    },
  }),
}

export default ProjectPageMessages
